export {add, subtract, multiply, divide};

function add (x, y) {
    const symbol = "+"
    const x = x
    const y = y
    return x + y;
}

function subtract (x, y) {
    return x - y;
}

function multiply (x, y) {
    return x * y;
}

function divide (x, y) {
    return x / y;
}